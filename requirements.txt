# Streamlit Community Cloud installs from this file. It lists direct
# dependencies with the same ranges as pyproject.toml rather than a fully
# pinned transitive lock, because the cloud builder runs a different Python
# version than local development and pinned wheels for one do not always
# resolve on the other.
langchain>=1.2,<2
langchain-openai>=1.1,<2
langgraph>=1.0,<2
langsmith>=0.4,<1
pydantic>=2.9,<3
python-dotenv>=1.0,<2
httpx>=0.27,<1
streamlit>=1.49,<2
